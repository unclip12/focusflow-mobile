import 'package:flutter/material.dart';

class FMGEScreen extends StatelessWidget {
  const FMGEScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FMGE')),
      body: const Center(child: Text('FMGE — coming in Turn 3')),
    );
  }
}
