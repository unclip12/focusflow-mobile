import 'package:flutter/material.dart';

class TimeLoggerScreen extends StatelessWidget {
  const TimeLoggerScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Time Logger')),
      body: const Center(child: Text('Time Logger — coming in Turn 3')),
    );
  }
}
